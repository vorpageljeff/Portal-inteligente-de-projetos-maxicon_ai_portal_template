services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: project_portal
      POSTGRES_USER: project_portal
      POSTGRES_PASSWORD: project_portal
    ports:
      - "5432:5432"
    volumes:
      - project_portal_db:/var/lib/postgresql/data
volumes:
  project_portal_db:
